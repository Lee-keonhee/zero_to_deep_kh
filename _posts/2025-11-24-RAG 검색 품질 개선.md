---
layout: post
title: RAG 검색 품질 개선 방법
summary: Retrieve 품질개선
author: keonhee
date: 2025-11-24 09:00:00 +0900
category: Deeplearning, NLP, RAG
keywords: 딥러닝
permalink: /blog/RAG_retriever_improve/
usemathjax: true
thumbnail: /assets/img/posts/overview_of_NLP_1.png
---
# RAG 검색 품질 개선 가이드

## 1. 검색 개수(k) 늘리기 ⭐ (가장 쉬움)

```python
# 현재
retrieval_k = 5

# 개선
retrieval_k = 10  # 또는 15

# config.yaml
retrieval:
  default_k: 10
  conditional_k: 20  # 조건추출 같은 경우
```

**효과:** 더 많은 후보 → Reranking 후 좋은 청크 선택 확률 ↑

---

## 2. 하이브리드 검색 (Vector + BM25) ⭐⭐

```python
from rank_bm25 import BM25Okapi

def hybrid_search(query, vectorstore, k=10):
    # 1. 벡터 검색
    vector_results = vectorstore.similarity_search(query, k=k*2)
    
    # 2. BM25 키워드 검색
    all_docs = vectorstore.get_all_documents()
    tokenized_docs = [doc.split() for doc in all_docs]
    bm25 = BM25Okapi(tokenized_docs)
    bm25_scores = bm25.get_scores(query.split())
    
    # 3. 점수 결합 (가중 평균)
    combined_results = []
    for doc in vector_results:
        vector_score = doc.metadata['score']
        bm25_score = bm25_scores[doc.id]
        final_score = 0.7 * vector_score + 0.3 * bm25_score
        combined_results.append((doc, final_score))
    
    # 4. 재정렬
    combined_results.sort(key=lambda x: x[1], reverse=True)
    return [doc for doc, score in combined_results[:k]]
```

**효과:**
- Vector: 의미 유사도 (동의어 인식)
- BM25: 정확한 키워드 매칭
- 둘의 장점 결합!

---

## 3. 청크 크기 최적화 ⭐

```python
# 현재 설정 확인 필요
chunk_size = 500  # 너무 작을 수 있음

# 개선
chunk_size = 800-1000
chunk_overlap = 200  # 중복으로 맥락 유지
```

**테스트 방법:**
```python
# 여러 크기로 실험
for size in [500, 800, 1000, 1500]:
    # 벡터DB 재생성
    # 검색 정확도 측정
```

---

## 4. 쿼리 확장 (Query Expansion) ⭐⭐

```python
def expand_query(query):
    """쿼리에 동의어/관련어 추가"""
    
    prompt = f"""
    다음 쿼리를 검색에 최적화하기 위해 확장하세요:
    
    원본: {query}
    
    다음을 포함하여 확장하세요:
    1. 동의어 (예: "사업" → "프로젝트", "과제")
    2. 관련 용어 (예: "보안" → "정보보안", "데이터 보호")
    3. 약어 풀이 (예: "AI" → "인공지능")
    
    확장된 쿼리만 출력하세요.
    """
    
    expanded = llm.generate(prompt)
    return expanded

# 사용
original_query = "외부 용역 보안 특약"
expanded_query = expand_query(original_query)
# → "외부 용역 보안 특약 정보보안 데이터 보호 비밀유지 계약 NDA"

results = vectorstore.search(expanded_query)
```

---

## 5. Hypothetical Document Embeddings (HyDE) ⭐⭐⭐

```python
def hyde_search(query, vectorstore, k=10):
    """가상 답변을 만들어서 검색"""
    
    # 1. LLM에게 가상 답변 생성 요청
    prompt = f"""
    다음 질문에 대한 답변이 문서에 어떻게 쓰여있을지 예상해서 작성하세요:
    
    질문: {query}
    
    예상 답변 (2-3문장):
    """
    
    hypothetical_answer = llm.generate(prompt)
    
    # 2. 가상 답변으로 검색 (질문 대신!)
    results = vectorstore.similarity_search(hypothetical_answer, k=k)
    
    return results
```

### 왜 효과적?

```
질문: "외부 용역 보안 특약이 뭐야?"
→ 임베딩: [0.2, 0.5, ...] (질문 형태)

문서: "외부 용역 보안 특약은 계약 시 ..."
→ 임베딩: [0.3, 0.4, ...] (서술 형태)

→ 유사도 낮음 ❌

---

HyDE 가상 답변: "외부 용역 보안 특약은 계약 시 준수해야 할..."
→ 임베딩: [0.29, 0.41, ...] (서술 형태)

문서: "외부 용역 보안 특약은 계약 시 ..."
→ 임베딩: [0.3, 0.4, ...] (서술 형태)

→ 유사도 높음 ✅
```

---

## 6. Parent Document Retriever ⭐⭐

```python
# 작은 청크로 검색하되, 큰 청크 반환
class ParentDocumentRetriever:
    def __init__(self, vectorstore):
        # 작은 청크 (검색용)
        self.small_chunks = create_chunks(docs, size=300)
        
        # 큰 청크 (LLM에 전달용)
        self.large_chunks = create_chunks(docs, size=1000)
        
        # 매핑 관계 저장
        self.chunk_mapping = {}  # small_id → large_id
    
    def search(self, query, k=5):
        # 1. 작은 청크로 검색 (정확도 높음)
        small_results = self.vectorstore.search(query, k=k)
        
        # 2. 해당하는 큰 청크 반환 (맥락 풍부)
        large_results = []
        for small in small_results:
            large_id = self.chunk_mapping[small.id]
            large_results.append(self.large_chunks[large_id])
        
        return large_results
```

---

## 7. 메타데이터 필터링 개선 ⭐

```python
# 현재: 엄격한 필터
{'발주 기관': ['고려대학교']}
s
# 개선: 유연한 필터
def flexible_filter(filters):
    """부분 매칭 허용"""
    where_clause = {}
    
    for key, values in filters.items():
        if key == '발주 기관':
            # OR 조건 + 부분 매칭
            where_clause['$or'] = [
                {key: {'$regex': f'.*{v}.*'}} 
                for v in values
            ]
    
    return where_clause

# 검색
results = vectorstore.search(
    query,
    where=flexible_filter({'발주 기관': ['고려대']})
)
# → "고려대학교", "고려대", "KU" 모두 매칭!
```

---

## 8. Reranking 모델 업그레이드 ⭐

```python
# 현재
reranker = "BAAI/bge-reranker-base"

# 개선
reranker = "BAAI/bge-reranker-v2-m3"  # 더 최신 모델
# 또는
reranker = "cross-encoder/ms-marco-MiniLM-L-12-v2"  # 영어 강함
```

---
